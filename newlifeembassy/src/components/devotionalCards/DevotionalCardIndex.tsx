import { Box, Button, Typography } from "@mui/material";
import React from "react";

const DevotionalCard: React.FC = () => {
  return (
    <Box>
      <Typography>DevotionalCard</Typography>
      <Button variant="outlined">Home</Button>
    </Box>
  );
};

export default DevotionalCard;
