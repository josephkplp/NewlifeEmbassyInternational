import { Box, Button, Typography } from "@mui/material";
import React from "react";

const HaroIndex: React.FC = () => {
  return (
    <Box>
      <Typography>HaroIndex</Typography>
      <Button variant="outlined">Home</Button>
    </Box>
  );
};

export default HaroIndex;
